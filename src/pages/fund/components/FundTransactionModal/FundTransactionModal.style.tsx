import styled from 'styled-components';
import { DV } from 'components/basic/designVariables';

export const FundTransactionModalWrapper = styled.div`
  display: flex;
  flex-direction: column;
  padding: ${DV.sizes.basePadding * 2}px;
`;
