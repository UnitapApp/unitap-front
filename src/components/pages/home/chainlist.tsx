import * as React from 'react';

// interface Prop {
//     data: object
// }

// interface chainList {
//     icon: string;
//     name: string;
//     chain_id: number;
//     symbol: string;
// }[]

const ChainList = (data: any) => {
    
    
    return ( 
    <div> 
        {/* { data.data.map((x: object) => {
            return (
                <div key={x.chain_id}>{x.name}</div>
            )
        }) } */}
    </div>
          );
}


 
export default ChainList;