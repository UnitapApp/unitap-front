import Modal from "@/components/ui/Modal/modal";

const AddWalletModal = () => {
  return (
    <Modal closeModalHandler={() => {}} isOpen>
      salam
    </Modal>
  );
};

export default AddWalletModal;
